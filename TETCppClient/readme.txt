This project compiles the c++ version of The Eye Tribe SDK.

Dependencies:
    - links with homebrew installed boost version 1.55 (usr/local/Cellar), change "other linker options" to link with different version of boost.